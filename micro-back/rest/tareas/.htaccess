<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Si el archivo no existe físicamente, mándalo al index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [QSA,L]
</IfModule>